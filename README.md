install python 
install necessary libraries : 
1. python3 -m venv .venv -> creating .vnev file 
2. source .venv/bin/activate -> activate virtual enviroment
3. pip install pandas openpyxl -> installing dependencies 
4. pip install flask pandas openpyxl -> install for flast api
5. run on http://127.0.0.1:5000 in your browser
